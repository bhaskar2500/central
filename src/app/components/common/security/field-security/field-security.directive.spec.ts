import { FieldSecurityDirective } from './field-security.directive';

describe('FieldSecurityDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldSecurityDirective();
    expect(directive).toBeTruthy();
  });
});
